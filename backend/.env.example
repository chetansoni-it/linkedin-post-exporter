# Database Configuration
# Default Values: localhost, 5432, root, root, test_db
DB_HOST=localhost
DB_PORT=5432
DB_USER=root
DB_PASSWORD=root
DB_NAME=test_db

# Email Configuration (for sending emails)
# Requires an App Password if using Gmail (https://myaccount.google.com/apppasswords)
SENDER_EMAIL=your_email@gmail.com
SENDER_PASSWORD=your_app_password
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Portfolio Link (optional - included in email footer)
PORTFOLIO_LINK=https://your-portfolio-link.com
